javascript:(function(){    var url = prompt("Enter YouTube Video URL:");    if (!url) return;    // Extract video ID from URL    var videoId = url.split('v=')[1];    if (!videoId) {        videoId = url.split('/').pop();    }    var downloadUrl = "https://yt1s.com/en31/" + videoId; // Using yt1s.com as a downloader    window.open(downloadUrl, "_blank");})();
